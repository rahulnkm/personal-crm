# .env.example — copy to ~/.crm/.env (preferred) or ./.env and fill in.
# Local dev values come from `supabase start` output.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SECRET_KEY=replace-with-sb_secret-or-local-service-key
